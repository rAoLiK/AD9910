# AD9910 数字锁相工程硬件 IO 接线规则

> 适用工程：STM32F407ZGT6 + AD9910 + TJC 串口屏 + OpenMV N6 + SSD1309 OLED
> 核对日期：2026-08-01
> 判定依据：实际初始化/驱动代码优先，其次为 CMake 编译定义，最后为 `AD9910.ioc`。

## 1. 使用范围与总原则

本文列出当前固件实际使用、动态配置或明确预留的全部外部 IO，并给出接线、电平、默认状态和排障规则。未列出的 STM32 GPIO 当前没有工程功能，不应仅凭芯片复用表自行接入；新增功能时必须同步修改固件、`AD9910.ioc` 和本文档。

接线前必须遵守以下原则：

1. STM32、AD9910 控制接口、OpenMV、串口屏、外部模拟前端、信号源和示波器必须有可靠的公共参考地。模拟地与数字地应按板级设计在单点或低阻抗区域汇接，避免高速数字回流穿过 ADC 前端。
2. STM32F407 的普通数字 IO 和 ADC 均按 3.3 V 域使用，任何 IO 都不得直接承受 5 V、负电压或超过 `VREF+` 的电压。
3. UART 是 3.3 V TTL/CMOS 串口，不是 RS-232 电平。TX 与 RX 必须交叉连接。
4. ADC 输入不能直接连接双极性信号源或 AD9910 裸输出，必须先限幅、偏置、缓冲，并保证在所有瞬态下严格处于 0～3.3 V。
5. `PE0`、`PE1` 只能驱动继电器驱动器或模拟开关的逻辑输入，禁止直接驱动继电器线圈或把模拟信号接到这两个脚。
6. 所有外设模块应先确认逻辑电平兼容。模块由 5 V 供电不代表其 UART/I2C/控制引脚可直接接入 STM32。
7. 上电、复位期间 MCU IO 可能短暂为高阻态或与正常运行态不同；对输出通路有严格安全要求时，必须由外部上拉/下拉、门控或继电器触点实现硬件失效安全，不能只依赖软件初始化。

## 2. 系统接线总览

| 设备/接口 | STM32 或 OpenMV 端 | 对端 | 方向 | 核心规则 |
|---|---|---|---|---|
| AD9910 软件 SPI | `PB10/PB11/PE13/PE14/PE15` | `SDIO/RESET/SCLK/CSB/IO_UPDATE` | STM32 → AD9910 | 3.3 V 逻辑，写入模式，无 SDO 读回 |
| 参考信号采样 | `PC0 / ADC1_IN10` | 信号源经调理后的参考采样 | 输入 | 0～3.3 V，中心约 1.65 V，低源阻抗 |
| DDS 反馈采样 | `PC1 / ADC2_IN11` | AD9910 输出经调理后的反馈采样 | 输入 | 0～3.3 V，中心约 1.65 V，低源阻抗 |
| Task5 DAC | `PA4 / DAC_OUT1` | Task5 信号选择器的 DAC 输入 | 输出 | 0～3.3 V 单调锯齿波，必要时外加缓冲 |
| 输出通路选择 | `PE1` | 继电器/通路驱动逻辑输入 | 输出 | 高=直通，低=DDS（当前编译极性） |
| Task5 信号源选择 | `PE0` | DDS/DAC 模拟选择器的 SEL | 输出 | 高=DDS，低=PA4 DAC |
| Task5 按键 | `PA0/PB9/PB8` | 三枚无源按键 | 输入 | PA0 按下接 3.3 V；PB9/PB8 按下接 GND |
| TJC 串口屏 | `PC6/PC7 / USART6` | 屏幕 RX/TX | 双向 | 115200、8N1、交叉连接、共地 |
| OpenMV 通信 | `PA2/PA3 / USART2` | OpenMV `P13/P14 / UART7` | 双向 | 115200、8N1、交叉连接、共地 |
| OpenMV OLED | OpenMV `P7/P8` | SSD1309 `SCL/SDA` | 双向 | 软件 I2C，100 kHz，地址 0x3C，拉到 3.3 V |
| SWD 调试 | `PA13/PA14` | ST-Link `SWDIO/SWCLK` | 双向 | 另接 GND、VTref，建议接 NRST |
| STM32 主时钟 | `PH0/PH1` | 8 MHz HSE 晶体网络 | 时钟 | 当前代码为晶体模式 `HSE_ON`，不是旁路时钟模式 |
| LSE 预留 | `PC14/PC15` | 32.768 kHz 晶体网络（可选） | 时钟 | `.ioc` 中预留，当前运行代码未启用 LSE |

## 3. STM32F407 全部已用 IO

### 3.1 主清单

| MCU IO | 模式 | 工程功能 | 有效电平/空闲态 | 对端与接线要求 |
|---|---|---|---|---|
| `PB10` | 高速推挽输出 | AD9910 `SDIO` | 数据线；初始低 | 接 AD9910 `SDIO`，当前仅写不读 |
| `PB11` | 高速推挽输出 | AD9910 主 `RESET` | 高脉冲复位；初始/空闲低 | 接 AD9910 `RESET`，不要误接 `IO_RESET` |
| `PE13` | 高速推挽输出 | AD9910 软件 `SCLK` | 初始/空闲低 | 接 AD9910 `SCLK` |
| `PE14` | 高速推挽输出 | AD9910 `CSB` | 低有效；初始/空闲高 | 接 AD9910 `CSB/CS` |
| `PE15` | 高速推挽输出 | AD9910 `IO_UPDATE` | 高脉冲提交寄存器；初始/空闲低 | 接 AD9910 `IO_UPDATE/UPD` |
| `PE0` | 推挽输出 | Task5 DDS/DAC 源选择 | 初始高；高=DDS，低=PA4 DAC | 接 3.3 V 兼容模拟开关/多路器 `SEL` |
| `PE1` | 推挽输出 | 直通/DDS 输出通路选择 | 初始高；高=直通，低=DDS | 接继电器驱动/通路控制输入，不能接线圈 |
| `PA0` | EXTI 上升沿输入，内部下拉 | Task5 对角线模式键 | 空闲低，按下高 | 无源按键跨接 `PA0` 与 3.3 V |
| `PB9` | EXTI 下降沿输入，内部上拉 | Task5 圆模式键 | 空闲高，按下低 | 无源按键跨接 `PB9` 与 GND |
| `PB8` | EXTI 下降沿输入，内部上拉 | Task5 ∞ 模式键 | 空闲高，按下低 | 无源按键跨接 `PB8` 与 GND |
| `PC0` | 模拟输入 | `ADC1_IN10`，参考输入采样 | 0～3.3 V | 接信号源调理通道输出 |
| `PC1` | 模拟输入 | `ADC2_IN11`，DDS 反馈采样 | 0～3.3 V | 接 AD9910 反馈调理通道输出 |
| `PA4` | 模拟输出 | `DAC_OUT1`，Task5 锯齿波 | 0～3.3 V 标称域 | 接 Task5 模拟选择器的 DAC 输入 |
| `PA2` | AF7 推挽输出 | `USART2_TX` | 115200 8N1 | 接 OpenMV `P13 / UART7_RX` |
| `PA3` | AF7 输入 | `USART2_RX` | 115200 8N1 | 接 OpenMV `P14 / UART7_TX` |
| `PC6` | AF8 推挽输出 | `USART6_TX` | 115200 8N1 | 接 TJC 屏 `RX` |
| `PC7` | AF8 输入 | `USART6_RX` | 115200 8N1 | 接 TJC 屏 `TX`；若屏幕 TX 为 5 V，必须电平转换 |
| `PA13` | SWD | `SWDIO` | 调试接口 | 接 ST-Link `SWDIO`，不要复用为普通 IO |
| `PA14` | SWD | `SWCLK` | 调试接口 | 接 ST-Link `SWCLK`，不要复用为普通 IO |
| `PH0` | HSE OSC_IN | 8 MHz 主晶体输入 | 模拟振荡引脚 | 接 8 MHz 晶体/谐振器网络，不接普通 IO |
| `PH1` | HSE OSC_OUT | 8 MHz 主晶体输出 | 模拟振荡引脚 | 接 8 MHz 晶体/谐振器网络，不接普通 IO |
| `PC14` | LSE OSC32_IN 预留 | 32.768 kHz 晶体输入 | 当前固件未启用 | 保留，不与其他信号共用 |
| `PC15` | LSE OSC32_OUT 预留 | 32.768 kHz 晶体输出 | 当前固件未启用 | 保留，不与其他信号共用 |

### 3.2 `.ioc` 与运行代码的差异

- `PA0`、`PB8`、`PB9` 由 `AppBoard_Init()` 在运行时配置，因此没有完整出现在 `AD9910.ioc` 的 `Mcu.Pin*` 清单中，但它们是当前固件的正式 IO，不能占作他用。新的 DDS 控制脚和 `PE0/PE1` 已同步写入 `.ioc`。
- `PC14/PC15` 在 `.ioc` 中标记为 LSE 外部晶体；当前 `SystemClock_Config()` 只启用 8 MHz HSE，没有启动 LSE。它们仍应作为预留时钟脚处理，除非同时修改 `.ioc`、时钟代码和硬件。
- TJC 驱动头文件存在其他 UART 的兼容默认值，但当前 CMake 明确编译为 `huart6/USART6`，实际接线必须使用 `PC6/PC7`。
- AD9910 驱动支持硬件 SPI 和读回，但当前编译使用软件 SPI，且 `AD9910_ENABLE_READBACK=0`，不得按硬件 SPI/MISO 接法推断。

## 4. AD9910 接线规则

### 4.1 必接数字控制线

| STM32 | AD9910 | 方向 | 规则 |
|---|---|---|---|
| `PB10` | `SDIO` | STM32 → AD9910 | 软件串行数据，当前仅写 |
| `PB11` | `RESET/RST` | STM32 → AD9910 | AD9910 主复位，高有效 |
| `PE13` | `SCLK` | STM32 → AD9910 | 软件 SPI 时钟，空闲低 |
| `PE14` | `CSB/CS` | STM32 → AD9910 | 低有效，初始化即保持高 |
| `PE15` | `IO_UPDATE/UPD` | STM32 → AD9910 | 写寄存器后用高脉冲使配置生效 |
| `GND` | `DGND/模块 GND` | 公共参考 | 必须共地 |

当前最小控制接口就是以上五根信号线加地线。`SOFT_SDO` 未配置，SPI 读回接口不可用。

### 4.2 Profile 与可选引脚

- `PROFILE0/PROFILE1/PROFILE2` 没有接 STM32。当前工程始终使用 Profile 0，因此这三脚必须全部可靠接地，不能悬空；驱动头文件已明确注明悬空会导致 AD9910 不工作。
- `IO_RESET` 未接 MCU。它只复位串行接口状态机，不等同于 `RESET`。
- `DR_HOLD`、`DR_CTL`、`DR_OVER`、`OSK` 均未接 MCU，当前数字斜坡、OSK 外部控制和 DRG 越界读取不依赖这些脚。
- 若使用带完整外围电路的 AD9910 模块，按模块板上的默认上下拉处理未用脚；若使用裸芯片或模块没有偏置，所有未使用的数字输入都必须按 AD9910 数据手册固定到非活动电平，禁止悬空。`DR_OVER` 是 AD9910 输出，不应外部强拉。

### 4.3 参考时钟与模拟输出

- 固件假定 AD9910 参考时钟为 **40 MHz**，内部 PLL 倍频为 **25**，系统时钟为 **1 GHz**。这不是 STM32 输出的时钟；AD9910 板上的晶振或外部参考源必须真实为 40 MHz。若硬件是 25 MHz 等其他频率，必须先修改固件参数，不能直接使用。
- AD9910 DAC 是电流型开漏输出。裸 `IOUT` 不能直接接 STM32 ADC 或普通高阻电压输入；应使用模块规定的 50 Ω 负载、变压器/滤波和放大网络形成电压输出。
- `PC1` 取的是放大器前的 DDS 反馈，只用于频率/相位闭环，不代表最终输出端的绝对幅度。最终幅度必须在示波器的真实 Y 输出端校准。

## 5. ADC 与模拟前端接线

### 5.1 两路 ADC 的定义

| ADC 通道 | 功能 | 建议取样点 |
|---|---|---|
| `PC0 / ADC1_IN10` | 参考信号 | 信号发生器输入进入系统后、DDS 处理前的参考支路 |
| `PC1 / ADC2_IN11` | DDS 反馈 | AD9910 输出滤波/必要缓冲后、最终输出放大器前的反馈支路 |

两路为 12 bit 双重规则同步采样，由 TIM2 TRGO 触发，采样时间仅 3 cycles，最高设计采样率约 2.4 MSPS。接线必须满足：

1. 两路在直流和交流瞬态下均严格限制在 0～3.3 V，推荐正弦中心偏置约 1.65 V（约 ADC 中值 2048）。
2. 两路前端应采用相近的增益、群延迟和滤波结构，否则硬件相移会直接进入锁相误差。
3. 3-cycle 采样要求低源阻抗，推荐运放缓冲；高阻分压器不能在没有缓冲的情况下直接驱动 ADC。
4. 推荐在 ADC 前设置限流电阻、钳位/过压保护和合适的抗混叠滤波。保护器件不能在正常幅度下产生明显非线性。
5. 信号发生器若按 50 Ω 源标定，分支到示波器和 ADC 前端时要统一考虑终端阻抗，避免重复 50 Ω 终端导致幅度减半。
6. 禁止将以 0 V 为中心的 ±正弦、BNC 双极性输出或 AD9910 裸差分输出直接接到 `PC0/PC1`。

## 6. Task5 DAC、继电器与模拟选择器

### 6.1 PA4 DAC

`PA4 / DAC_OUT1` 由 TIM6 + DMA1 Stream5 输出 100 点单调递增锯齿波：

- 短按任一 Task5 模式键：1 kHz；
- 长按达到 400 ms：10 kHz；
- DAC 码表范围为 `0x000～0xFFF`，输出缓冲已开启，电压域标称为 0～3.3 V；
- `PA4` 应接到 PE0 所控制模拟选择器的 DAC 输入端；若要驱动长线、低阻负载或示波器 50 Ω 端口，必须外加合适的运放缓冲，禁止让 MCU DAC 直接驱动 50 Ω。

### 6.2 PE1 通路控制

当前 `APP_RELAY_DIRECT_ACTIVE_HIGH=1`：

| `PE1` | 逻辑通路 |
|---:|---|
| 高 | 直通路径 |
| 低 | DDS 路径 |

若实际继电器驱动板为低有效或触点映射相反，必须修改硬件映射或 `APP_RELAY_DIRECT_ACTIVE_HIGH`，不能只交换屏幕语义。PE1 接继电器驱动器/光耦/逻辑输入，不得直接接线圈；驱动感性负载必须由外部晶体管和续流器件完成。

### 6.3 PE0 信号源控制

| `PE0` | Task5 总信号源 |
|---:|---|
| 高 | DDS/Task1～4 支路 |
| 低 | PA4 DAC 锯齿波支路 |

`PE0` 接模拟开关或多路选择器的逻辑选择脚。该器件的逻辑高门限必须兼容 3.3 V，模拟电源及信号范围必须覆盖 DDS/DAC 的实际电压，且通道导通电阻、带宽、失真满足题目频率范围。

常见运行状态如下：

| 状态 | PE1 | PE0 | 说明 |
|---|---:|---:|---|
| AppBoard 初始化完成/安全态 | 高 | 高 | 直通、DDS 支路待命 |
| Task1～4 直通 | 高 | 高 | 不走 DDS |
| Task1～4 锁相 | 低 | 高 | 输出 DDS |
| Task5 粗识别锯齿波 | 通常高 | 低 | PA4 锯齿波进入输出，OpenMV 等待有效 XY 展宽后判频 |
| Task5 DDS 搜索/视觉锁定 | 低 | 高 | 输出 DDS |
| Task5 错误恢复 | 可能保留故障前 PE1 | 低 | 重新显示本次选择的 DAC 锯齿波，便于诊断 |

注意上电时序：`MX_GPIO_Init()` 在切换输出模式前，先将 `PE0` 置高选择 DDS 支路、`PE1` 置高选择直通路径、`PE14/CSB` 置高，并将 `PB10/PB11/PE13/PE15` 置低；随后 `AppBoard_Init()` 再次确认 `PE0/PE1` 的安全状态。芯片复位释放到执行 `MX_GPIO_Init()` 之前，这些脚仍可能短暂为高阻态；若模拟输出绝不能在启动阶段误切换，仍应增加外部默认偏置或输出使能门控。

## 7. Task5 三枚实体按键

### 7.1 接法

```text
3.3 V ──[无源按键]── PA0      （内部下拉，按下为高）

PB9   ──[无源按键]── GND      （内部上拉，按下为低）
PB8   ──[无源按键]── GND      （内部上拉，按下为低）
```

| IO | 模式 | 功能 | 按下电平 | EXTI |
|---|---|---|---:|---|
| `PA0` | 内部下拉 | 对角线/直线模式 | 高 | 上升沿、EXTI0 |
| `PB9` | 内部上拉 | 圆模式 | 低 | 下降沿、EXTI9_5 |
| `PB8` | 内部上拉 | ∞ 模式 | 低 | 下降沿、EXTI9_5 |

按键只使用无源触点，禁止外接 5 V。软件另有 50 ms 去抖；按住时间小于 400 ms 选择 1 kHz，达到或超过 400 ms 选择 10 kHz。三枚按键仅在 Task5 启用，Task1～4 中无效。

## 8. 串口接线

### 8.1 TJC 串口屏：USART6

```text
STM32 PC6 / USART6_TX  ─────>  TJC RX
STM32 PC7 / USART6_RX  <─────  TJC TX
STM32 GND              ──────  TJC GND
```

- 参数：115200 baud、8 data bits、no parity、1 stop bit、无硬件流控。
- `PC7` 不耐受超出 MCU 允许范围的电平。如果屏幕 TX 为 5 V TTL，必须在 TJC TX → PC7 方向使用可靠的电平转换或分压；不能直接连接。
- 屏幕电源按具体型号额定值供电，不要默认由 STM32 3.3 V 引脚供电。
- 当前蜂鸣提示通过 TJC 串口命令 `beep 500` 使用屏幕内部蜂鸣器，工程没有独立的 STM32 蜂鸣器 IO。

### 8.2 OpenMV N6：USART2 ↔ UART7

```text
STM32 PA2 / USART2_TX  ─────>  OpenMV P13 / UART7_RX
STM32 PA3 / USART2_RX  <─────  OpenMV P14 / UART7_TX
STM32 GND               ─────  OpenMV GND
```

- 两端参数均为 115200、8N1、无流控。
- OpenMV 固件固定使用 UART7：`P14=TX`、`P13=RX`；不得把 P13/P14 按同名直连，必须按 TX→RX 交叉。
- OpenMV 可由其合规电源或 USB 供电，但多电源并用前必须确认不会反向灌电；无论如何都必须与 STM32 共地。
- 相机与示波器之间没有电气连接，OpenMV 通过镜头观察 XY 绘图区。

## 9. OpenMV 外接 SSD1309 OLED

当前比赛接线和快速启动参数固定为：

```text
OpenMV P7  ─────> OLED SCL
OpenMV P8  <────> OLED SDA
OpenMV GND ─────  OLED GND
OLED VCC   ─────  按模块额定电源供电
```

| 项目 | 当前值 |
|---|---|
| 控制器 | SSD1309 |
| 总线 | OpenMV `SoftI2C` |
| SCL/SDA | `P7/P8` |
| 频率 | 100 kHz |
| 地址 | `0x3C` |

SCL/SDA 必须有到 **3.3 V** 的上拉；若 OLED 模块自带上拉到 5 V，必须改为 3.3 V 或做双向电平转换。当前 `OLED_FAST_START=True` 时不会扫描其他引脚或地址，启动即按 P7/P8、100 kHz、0x3C 通信。代码中的 P4/P5 和 0x3D 仅属于关闭快速启动时的通用兼容路径/报错提示，不是本工程当前推荐接法。

## 10. 时钟、调试和不需要外接的资源

### 10.1 STM32 HSE

当前 `SystemClock_Config()` 使用 `RCC_HSE_ON`，参数假定 8 MHz 晶体，经 PLL 产生 144 MHz 系统时钟：

- `PH0-OSC_IN` 与 `PH1-OSC_OUT` 接 8 MHz 晶体/谐振器及匹配负载电容；
- 晶体回路要短、对称、远离高速翻转线，不要接测试长线或普通 GPIO 负载；
- 若改用有源时钟直接送入 PH0，必须按 STM32 要求改为 HSE bypass 配置，不能保持当前 `HSE_ON` 配置直接替换。

### 10.2 SWD

推荐 ST-Link 接线：

```text
ST-Link SWDIO ─── PA13
ST-Link SWCLK ─── PA14
ST-Link GND   ─── GND
ST-Link VTref ─── 目标板 3.3 V（电压检测，不应误作大电流电源）
ST-Link NRST  ─── NRST（推荐）
```

下载和调试期间保持 PA13/PA14 专用，不要并接 LED、继电器或其他强驱动负载。

### 10.3 复位、启动与模拟电源参考

- `NRST` 为低有效复位脚，应接板上复位网络；建议同时接到 ST-Link NRST，便于“硬复位下连接”。不要让外部设备长期拉低。
- `BOOT0` 正常运行必须保持低电平，建议使用可靠下拉；只有进入 STM32 系统 Bootloader 时才临时拉高，禁止悬空。
- `VDDA/VREF+` 的实际电压决定 ADC 满量程。本文所有 0～3.3 V 规则均基于模拟参考为 3.3 V；`VSSA` 必须可靠接模拟地并按板级要求去耦/滤波。
- `VCAP`、`VBAT`、各组 `VDD/VSS` 属于 MCU 基础供电网络而非应用 IO，应严格按 STM32F407ZGT6 硬件设计要求连接，不能拿来给外设供电或作为测试输出。

### 10.4 内部资源，无外部引脚

以下资源只在 MCU 内部使用，不需要也没有对应接线：

- TIM2：产生 ADC1/ADC2 同步采样 TRGO；
- TIM6：产生 PA4 DAC DMA 更新 TRGO；
- DMA2 Stream0：搬运 ADC 双重模式数据；
- DMA1 Stream5：搬运 PA4 锯齿波数据；
- SysTick、ADC/UART/DMA 中断。

### 10.5 当前明确未使用的接口

- STM32 硬件 SPI、STM32 I2C、USB、CAN、以太网和 SDIO 均未参与当前应用接线；
- AD9910 `SDO` 读回线未使用；
- 没有独立 MCU 蜂鸣器、状态 LED 或外部定时器输出引脚；
- 未列出的普通 GPIO 默认不承担工程功能，但在复用前仍需检查启动脚、板载外设和 PCB 实际连接。

## 11. 示波器与验收接线

Task5 的 OpenMV 识别依赖示波器 XY 绘图。推荐验收连接为：

- 示波器 X（通常 CH1）：参考/输入信号；
- 示波器 Y（通常 CH2）：最终被选择的输出信号；
- 示波器地与信号源、模拟板地共地；
- OpenMV 镜头固定对准完整 XY 绘图区，避免反光、遮挡和自动构图漂移。

示波器探头/BNC 的具体取样点属于模拟板布线，不由 MCU 代码命名。应确认 X 取输入参考、Y 取最终输出，而不是误接 `PC0/PC1` 的偏置后 ADC 节点。无有效输入时 XY 区域应为居中窄竖线；接入有效输入后轨迹产生稳定横向宽度，OpenMV 才进入频率识别。

## 12. 上电前逐项检查表

1. 所有设备已经共地，且没有通过示波器地夹造成意外短路。
2. STM32、OpenMV 和 AD9910 数字控制线均为兼容的 3.3 V 逻辑，没有 5 V 反灌。
3. `PC0/PC1` 静态约为 1.65 V，动态峰值始终位于 0～3.3 V。
4. AD9910 `PROFILE0/1/2` 全部可靠接地，`CS/RESET/IO_UPDATE/SCLK/SDIO` 未接错。
5. AD9910 实际参考时钟确为 40 MHz，输出端具备正确的 50 Ω/变压器/滤波网络。
6. UART 已交叉：PC6→TJC RX、PC7←TJC TX；PA2→OpenMV P13、PA3←OpenMV P14。
7. OLED 使用 P7=SCL、P8=SDA、地址 0x3C，I2C 上拉接 3.3 V。
8. PA0 按键接 3.3 V；PB9、PB8 按键接地；三者都没有外接 5 V。
9. PE0、PE1 只接逻辑控制端，继电器线圈有独立驱动和感性负载保护。
10. PA4 没有直接驱动 50 Ω 负载，必要的 DAC 缓冲已经安装。
11. PA13/PA14、PH0/PH1、PC14/PC15 没有被其他功能占用。
12. 上电后先确认安全态和串口通信，再接入信号源；最后检查 XY 图像、判频和锁定。

## 13. 维护时的核对源

引脚或接线有变更时，至少同步核对以下文件：

- `AD9910.ioc`：CubeMX 静态引脚和外设映射；
- `Core/Src/gpio.c`、`adc.c`、`dac.c`、`usart.c`：实际 HAL 初始化；
- `Usr/AD9910/ad9910.h`：AD9910 软件 SPI 和控制脚；
- `Usr/APP/app_board.c`：PE0、PE1 和三枚 Task5 按键；
- `CMakeLists.txt`：TJC 实际 UART 编译映射；
- `openmv/OpenMV_main_task5_uart.py`：OpenMV UART7 与 OLED P7/P8 参数。

如果上述文件发生冲突，以最终参与构建并在运行时执行的代码为准，同时修正其他配置和本文档，避免产生两套接线定义。
