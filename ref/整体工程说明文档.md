# 完整工程功能说明

## 1 工程外设配置

自行查阅初始化代码，搞清楚初始化了哪些外设

## 2 工程背景

该工程主要是解决2026年F题的单片机工程，具体的题目要求见`./F题`

## 3 工程大纲

主要思路是使用ADC读取输入的信号，并使用DDS输出信号后读回进行锁相，整体工程主要被分为三个部分：

1. 任务1：信号直通输出部分，配置一个IO，选择继电器，高和低电平含义待定，使用宏定义进行配置
2. 任务2-4：此时继电器选择另一路DDS输出的通道，进行实时锁相并控制相位和幅值，注意锁相使用的信号与真实DDS输出的信号恰好是反向的，锁相时记得对相位进行180°补偿。并且注意使用宏定义配置DDS信号经过放大后的满幅值，经过这一宏定义反推出DDS的幅度应该设置为多少以满足题目要求。
3. 任务5：此时需要用摄像头返回的指令进行锁相，这一部分先不写，只做屏幕逻辑的保留。注意这时候要配置单片机板子上的3个按键进行三个模式（PA0（接3.3V）\PB9（接地）\PB8（接地）注意不同按钮的硬件连接不同）的选择和进入，使用中断处理按钮事件（只有进入这个中断后才进行使能）

### 3.2 屏幕工程控件和工程流程说明

现在已经写好了屏幕工程的框架

一共有以下页面：

1. page0（menu）
2. page1（Task1-4）
3. page2（Task5）

需要与单片机交互的控件说明如下：

#### Page0（menu）

1. 按钮控件：

   b1：按下后由单片机发送页面指令跳转到 page1，等待选择 Task1-4

   b3：按下后由单片机发送页面指令跳转到 page2

#### Page1（Task1-4）

按题目要求进行直通、锁相和幅度输出。

1. 按钮控件：
   1. b0：exit返回，返回到page0，系统回到安全初始状态，DDS关闭输出
   2. b1：Task2：满足题目要求2的锁相输出
   3. b2：Task3：满足题目要求3的锁相输出
   4. b3：Task4-1：保持最近的Task1/2/3图形，Y轴幅值为2div
   5. b4：Task4-2：保持最近的Task1/2/3图形，Y轴幅值为4div
   6. b5：Task4-3：保持最近的Task1/2/3图形，Y轴幅值为6div
   7. b6：Task4-4：保持最近的Task1/2/3图形，Y轴幅值为8div
   8. b7：Task1输入输出直通

#### Page2（Task5）

这一部分预留摄像头锁相流程；当前不写锁相逻辑，仅在屏幕上输出状态。

1. 按钮控件：

   b0：exit返回，返回到page0，系统回到安全初始状态，DDS关闭输出

2. 文本控件：

   t2：显示当前的输出状态

Task5 页面没有开始按钮；PA0、PB9、PB8 三个板上按键分别选择并进入
三个预留自动模式，且仅在 Task5 状态下使能 EXTI。

## 4 工程要求

1. 采用状态机skill中的工程屏幕集成方案，要求进一步提升代码的可读性和可维护性
2. 仔细参考题目要求
3. 使用串口1作为屏幕的串口，demo中的串口功能弃之不用
